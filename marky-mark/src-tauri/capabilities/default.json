{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Markimark main window: fs for documents/sidecars/themes, dialogs, window control",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-set-title",
    "core:window:allow-close",
    "core:window:allow-destroy",
    "core:event:allow-listen",
    "dialog:default",
    {
      "identifier": "opener:allow-open-path",
      "allow": [{ "path": "$APPCONFIG" }, { "path": "$APPCONFIG/**" }]
    },
    "fs:allow-read-text-file",
    "fs:allow-write-text-file",
    "fs:allow-exists",
    "fs:allow-remove",
    "fs:allow-mkdir",
    "fs:allow-read-dir",
    "fs:allow-watch",
    {
      "identifier": "fs:scope",
      "allow": [{ "path": "**" }, { "path": "$APPCONFIG" }, { "path": "$APPCONFIG/**" }, { "path": "$HOME/**" }]
    }
  ]
}
